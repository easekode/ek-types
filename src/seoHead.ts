import { Image } from './common';

export interface SEOHead {
  title?: string;
  description?: string;
  image?: Image;
  url?: string;
  keyword?: string;
}

/* {
  metaTitle?: string;
  metaDescription?: string;
  metaKeywords?: string;
  openGraph?: string;
} */
